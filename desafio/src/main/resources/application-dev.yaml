spring:
  application:
    name: desafio
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/desafio?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: trcv1234
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true

server:
  port: 8086